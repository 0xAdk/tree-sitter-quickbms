================================================================================
only offset
================================================================================

goto FILE_OFFSET

---

(statement_list
	(goto_statement
		offset: (variable)))

================================================================================
with offset and file number
================================================================================

goto NAME_OFF MEMORY_FILE

---

(statement_list
	(goto_statement
		offset: (variable)
		file_number: (variable)))

================================================================================
with type
================================================================================

goto -4 0 SEEK_CUR

---

(statement_list
	(goto_statement
		offset: (variable)
		file_number: (variable)))
